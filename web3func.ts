import { 
    arbitrumSepoliaRPC, 
    optimismSepoliaRPC, 
    privateKey, 
    contractAddressArbitrumSepolia, 
    contractAddressOptimismSepolia,
    gelatoApiKey 
} from './config';

// to use the variables from config.ts

